## Install terraform on VM

## Configure aws cli with admin keys

## Copy terraform codes to Vm

## Init terraform

## Terraform apply
` terraform apply --auto-approve`

## Learn public ip 
` terraform show | grep public_ip `

## Wait 2 minutes for fastapi installation on EC2

## Open browser
http://<public_ip>:8002/docs

## Destroy
`  terraform destroy --auto-approve `